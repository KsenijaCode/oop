import { isValid } from "./isValid.js";

console.log(isValid.username());


const emailValidationResult = isValid.email('');
if (emailValidationResult === true) {
    return '👌';
} else {
    return string       // kas butent neatitinka
}

isValid.email('');

if (isValid.email()) {

}

if (!isValid.email('')) {
    return 'MISTAKE';
}
